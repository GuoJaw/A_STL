set和map——红黑树

priority_queue——优先级队列

二者的插入和删除时间复杂度都是O(lgN)

（1）所有的priority_queue的操作，都可以使用set/map实现
（2）但是set/map的操作，priority_queue不能完全替代。
================
set/map红黑树比prioity_queue优先级队列的好处：
    set/map
        自动排序：第一个元素和最后一个元素就是最大值和最小值
        可以直接获得最大值/最小值，并进行修改
    prioity_queue
        最大堆，每次只能获得最大值
        最小堆，每次只能获得最小值
